{
  "name": "@app/railway-scaler",
  "type": "module",
  "scripts": {
    "format:check": "pnpm -r --stream --aggregate-output run format:check",
    "types:check": "pnpm -r --stream --aggregate-output run types:check"
  },
  "devDependencies": {
    "lefthook": "2.0.15",
    "prettier": "3.8.1",
    "typescript": "5.9.3"
  }
}
